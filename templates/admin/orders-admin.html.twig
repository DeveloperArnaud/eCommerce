{% extends 'base.html.twig' %}

{% block nav %}
<nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <a class="navbar-brand" style="font-family: 'Permanent Marker', cursive; color : white; font-size: 25px" href="{{ path('products') }}">Sneak'Addict </a>  <p style="color : white; font-size : small">Admin<p>
</nav>
{% endblock %}
{% block body %}
<ul class="nav">
  <li class="nav-item">
    <a class="nav-link active" href=>Mon compte</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href={{path('product-admin')}}>Les produits</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href={{path('users-admin')}}>Les utilisateurs</a>
  </li>
    <li class="nav-item">
    <a class="nav-link" href={{path('orders-admin')}}>Les commandes</a>
  </li>
</ul>
    <div>
                   <table class="table">
                <thead>
                    <tr>
                    <th scope="col">N°</th>
                    <th scope="col">Client</th>
                    <th scope="col">Statut</th>
                    </tr>
                </thead>
        <tbody>
        {% for commande in users %}
         
        {% for detailc in commande.commandes %}
            <tr>
            <th scope="row">{{detailc.id}}</th>
            <td>{{commande.nom}} {{ commande.prenom}}</td>
            <td><select><option>{{detailc.statut}}  </option>
            <option>En cours de livraison  </option>
            <option>Livrée  </option></select></td>
            </tr>
            {% endfor %}
        {% endfor %}
        </tbody>
        </table>
           </div>
{% endblock %}